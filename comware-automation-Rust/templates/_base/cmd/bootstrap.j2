{# ============================================================ #}
{# 模板: _base/bootstrap.j2                                     #}
{# 用途: 全局兜底模板（极少使用）                               #}
{# ============================================================ #}
{#
# 警告：此模板仅在所有其他层级都找不到 bootstrap.j2 时才会使用。
# 通常情况下，你应该在 vendors/h3c/ 或更具体的目录中放置模板。
# ============================================================ #}

{{ "此设备没有匹配的 bootstrap 模板，请检查模板目录结构！" }}