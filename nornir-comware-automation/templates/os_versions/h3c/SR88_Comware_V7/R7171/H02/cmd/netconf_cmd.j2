{# ============================================================ #}
{# 补丁级：SR88_Comware_V7/R7171/H02/netconf_cmd.j2             #}
{# 用途：H02 热补丁 NETCONF 差异化配置                          #}
{# 适用：H3C SR88 Comware V7 R7171 + H02 热补丁                #}
{# ============================================================ #}
{#
# 此模板仅包含 H02 补丁相对于 R7171 分支的差异命令。
# 通用命令由父模板（R7171）提供，通过 {{ super() }} 继承。
#
# H02 补丁特性：
#   - 修复了 R7171 中 NETCONF 会话超时过早断开的问题
#   - 增加了 buffer 大小以支持大规模配置下发
#   - 关闭了调试日志（生产环境不需要）
#}

{% extends 'os_versions/h3c/SR88_Comware_V7/R7171//cmd/netconf_cmd.j2' %}

{% block custom_config %}
{# 继承 R7171 分支的所有配置 #}
{{ super() }}

{# ============================================================ #}
{# H02 特有：NETCONF 性能优化                                    #}
{# ============================================================ #}
display clock

{% endblock %}